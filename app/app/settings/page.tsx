import { EmptyState } from "@/components/dashboard/empty-state";

export default function SettingsPage() {
  return <EmptyState title="Settings" description="Profile, notifications, and integrations are on the way." actionLabel="View docs" />;
}
